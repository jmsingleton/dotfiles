general {
    lock_cmd = pidof hyprlock || hyprlock
    before_sleep_cmd = loginctl lock-session
    after_sleep_cmd = hyprctl dispatch dpms on
}

# Lock screen after 5 minutes
listener {
    timeout = 300
    on-timeout = loginctl lock-session
}

# Turn off display after 10 minutes
# DISABLED 2026-06-12: on amdgpu + HP Z27k G3 over HDMI, `dpms off` fully
# unplugs the monitor (DRM hot-unplug), which trips a null-deref crash in
# Hyprland 0.55.3 (getViewsForWorkspace on sole-monitor disconnect) and kills
# the whole session. Known upstream AMD+DPMS bug. Re-enable only after moving
# the monitor to DisplayPort/USB-C or once Hyprland fixes the disconnect path.
# listener {
#     timeout = 600
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }
